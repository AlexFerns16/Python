# using built-in functions on tuple

t = (12, 15, 13, 23, 22, 16, 17)

# len(t)
# max(t)
# min(t)
# sum(t)
# any(t)
# all(t)
# sorted(t)                     # returns a sorted list and not a tuple
# sorted(t, reverse=True)       # returns a sorted list and not a tuple
# print(list(reversed(t)))

# doesn't work -----------------------------------
# append
# insert
# remove
# pop
# sort
